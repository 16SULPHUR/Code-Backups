@{
    ViewData["Title"] = "Home Page";
}

@model IEnumerable<P_06.Models.Employee> 

<div class="text-center">
    <ul>
        @foreach (var employee in Model){
            <li>@employee.emp_id   @employee.name</li>
        }
    </ul>
</div>
